# Prisma Helpers

The Prisma schema lives in the root `prisma/` folder because Prisma CLI expects
that location by default. This `src/prisma/` folder is reserved for application
helpers related to database workflows.
